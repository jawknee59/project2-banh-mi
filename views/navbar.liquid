{% block content %}
<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <div class="container-fluid">
        <a href="/" class="navbar-brand">Home</a>
        <a href="/banhmis" class="nav-link text-light me-3">Banh Mi</a>
        {% if loggedIn %}
            <a class="nav-link text-warning me-3">Hello {{ username }} </a>
        {% endif %}
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                {% if loggedIn %}
                <a href="/auth/logout" class="nav-link text-light me-3">Favorites</a>
                <a href="/auth/logout" class="nav-link text-light me-3">Cart</a>
                <a href="/auth/logout" class="nav-link text-light me-3">Log Out</a>
                {% else %}
                <a href="/auth/login" class="nav-link text-light me-3">Sign In</a>
                <a href="/auth/signup" class="nav-link text-light me-3">Sign Up</a>
            {% endif %}
            </div>
    </div>
</nav>
{% endblock %}